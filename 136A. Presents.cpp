#include <bits/stdc++.h>
using namespace std;

#define for(n) for(int i = 1; i <= n; i++)
#define pb(n) push_back(n)

int main()
{
    int n, temp;
    cin >> n;

    int res[n];

    for(n){
        cin >> temp;
        res[temp] = i;
    }

     for(n) cout << res[i] << " ";

    return 0;
}
